import 'package:flutter/material.dart';

import 'farm_preview_screen.dart';

/// Compatibility alias — prefer [openFarmPreview] (농장) or [openLandMap] (내 땅).
void openLandPreview(BuildContext context) => openFarmPreview(context);
