/// W1 HealthKit / Health Connect — stub. Not in Release 1 (F1–F4).
class HealthBridge {
  Future<void> writeWorkout({
    required DateTime startedAt,
    required DateTime endedAt,
    required double distanceM,
  }) async {
    // no-op
  }
}
